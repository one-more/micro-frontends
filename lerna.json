{
  "packages": [
    "packages/*"
  ],
  "version": "independent",
  "npmClient": "npm",
  "command": {
    "publish": {
      "ignoreChanges": ["ignored-file", "*.md"],
      "npmClient": "npm",
      "useWorkspaces": true,
      "verifyAccess": false
    }
  }
}
